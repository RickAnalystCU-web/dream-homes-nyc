# Repository Curation Notes

This portfolio repository was curated from the larger course-project workspace.

The following working files were intentionally excluded:

- Root-level draft reports in `.docx` format
- Duplicate CSVs stored in `DreamHomes_Full_Synthetic_Dataset/`
- Archived zip files
- Empty placeholder folders with no project content
- A duplicate ERD PDF after the final copy was organized under `docs/erd/`

The repository retains the final PDFs, canonical SQL, ETL loader, synthetic dataset, metadata summaries, and dashboard screenshots needed to review the completed project.
