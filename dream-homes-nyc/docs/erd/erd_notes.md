# ERD Notes

The repository now includes an ERD PDF:

- `dreamhomes_nyc_erd.pdf`

For the current repository:

- The final schema is documented in `sql/schema/dreamhomes_postgres_schema.sql`.
- The ERD should match the finalized schema in `sql/schema/dreamhomes_postgres_schema.sql`.

Recommended manual review:

- Confirm whether the copied PDF is the final ERD version you want to publish.
