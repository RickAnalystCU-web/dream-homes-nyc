# Source File Review

This curated folder was built from the larger working project directory.

Files intentionally not copied here:

- Root-level draft reports in `.docx` format
- Duplicate CSVs stored in `DreamHomes_Full_Synthetic_Dataset/`
- Archived zip files
- Empty placeholder folders with no project content
- The original root-level ERD PDF, which was copied into `docs/erd/` for organization

Files that still need manual review before adding to GitHub:

- The duplicate root-level ERD PDF that still sits outside `docs/erd/`
- Whether you also want the editable `.pptx` presentation source inside `docs/final/`
- Whether `volume_summary.md` should remain as a separate metadata note in addition to `row_count_summary.md`

Reason for manual review:

- Most core deliverables are now present, but a few duplicate or optional source files still need a final naming and inclusion decision.
